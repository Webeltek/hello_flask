import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.bcal.app',
  appName: 'booking-calendar',
  webDir: 'dist/demos',
  bundledWebRuntime: false
};

export default config;
